import from './constants'
const initState = {
  todos: [],
  input: "",
};

function Reducer(action, type) {
  switch (action.type) {
    case: SET
  }
}

export { initState };
export default Reducer;
